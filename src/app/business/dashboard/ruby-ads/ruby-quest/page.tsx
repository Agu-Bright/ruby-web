'use client';

import Link from 'next/link';
import { Gem, Pause, Play, Sparkles } from 'lucide-react';
import { toast } from 'sonner';
import { usePauseRubyQuest, useResumeRubyQuest, useRubyQuestAnalytics, useRubyQuestCampaigns, type RubyRarity } from '@/lib/business-api/ruby-quest';

const tiers: Array<{ tier: RubyRarity; price: number; cadence: string; copy: string }> = [
  { tier: 'COMMON', price: 5000, cadence: 'Steady local discovery', copy: 'Invite nearby customers to discover your business.' },
  { tier: 'RARE', price: 12000, cadence: 'Higher-frequency discovery', copy: 'A stronger weekly presence for growing footfall.' },
  { tier: 'LEGENDARY', price: 30000, cadence: 'Premium discovery', copy: 'A high-visibility campaign reviewed before activation.' },
];

export default function RubyQuestPage() {
  const campaigns = useRubyQuestCampaigns();
  const analytics = useRubyQuestAnalytics();
  const refresh = () => { campaigns.refetch(); analytics.refetch(); };
  const pause = usePauseRubyQuest(() => { toast.success('Ruby Quest paused'); refresh(); });
  const resume = useResumeRubyQuest(() => { toast.success('Ruby Quest resumed'); refresh(); });
  const items = campaigns.data?.items ?? [];
  const stats = analytics.data as { issued?: number; collected?: number; collectionRate?: number; uniqueVisitors?: number } | null;
  return <main className="mx-auto max-w-6xl p-6"><Link href="/business/dashboard/ruby-ads" className="text-sm font-semibold text-gray-600">← Back to ads</Link><section className="mt-5 overflow-hidden rounded-3xl bg-gradient-to-br from-[#321242] via-[#6b1e55] to-ruby-red p-7 text-white shadow-lg"><div className="flex items-start gap-4"><div className="rounded-2xl bg-white/15 p-3"><Gem size={30} /></div><div><p className="text-xs font-semibold uppercase tracking-wider text-white/70">Real-world discovery</p><h1 className="mt-1 text-3xl font-bold">Ruby Quest</h1><p className="mt-2 max-w-xl text-sm leading-6 text-white/85">Drop a virtual gem at your business. Customers discover it nearby, visit to collect it, and receive a small Ruby+ reward.</p></div></div></section><section className="mt-6 grid gap-3 sm:grid-cols-4">{[['Issued', stats?.issued ?? 0], ['Collected', stats?.collected ?? 0], ['Collection rate', `${Math.round((stats?.collectionRate ?? 0) * 100)}%`], ['Unique visitors', stats?.uniqueVisitors ?? 0]].map(([label, value]) => <div key={String(label)} className="rounded-xl border bg-white p-4"><p className="text-xs text-gray-500">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div>)}</section><div className="mt-8 flex flex-wrap items-end justify-between gap-2"><div><h2 className="text-lg font-bold">Pick a tier</h2><p className="mt-1 text-sm text-gray-500">Ruby Quest subscriptions are not available yet.</p></div><span className="rounded-full bg-amber-50 px-3 py-1.5 text-xs font-semibold text-amber-800 ring-1 ring-amber-200">Coming soon</span></div><section className="mt-3 grid gap-4 md:grid-cols-3">{tiers.map((tier) => <article key={tier.tier} className="rounded-2xl border bg-white p-5 shadow-sm"><Sparkles size={18} className="text-ruby-red" /><h3 className="mt-3 font-bold">{tier.tier}</h3><p className="mt-1 text-sm text-gray-500">{tier.cadence}</p><p className="mt-4 text-2xl font-bold">₦{tier.price.toLocaleString('en-NG')}<span className="text-sm font-normal text-gray-500"> / week</span></p><p className="mt-3 text-sm text-gray-600">{tier.copy}</p><button disabled className="mt-5 w-full cursor-not-allowed rounded-lg bg-gray-100 px-4 py-2.5 text-sm font-semibold text-gray-500">Coming soon</button></article>)}</section><section className="mt-8 rounded-2xl border bg-white p-5"><h2 className="font-bold">Your Ruby Quest campaigns</h2>{campaigns.isLoading ? <p className="mt-3 text-sm text-gray-500">Loading campaigns…</p> : items.length ? <div className="mt-3 divide-y">{items.map((campaign) => <div key={campaign._id} className="flex flex-wrap items-center justify-between gap-3 py-4"><div><p className="font-semibold">{campaign.tier} quest <span className="ml-2 text-xs font-medium text-gray-500">{campaign.status}</span></p><p className="mt-1 text-xs text-gray-500">Ends {new Date(campaign.endDate).toLocaleDateString('en-NG')}</p></div>{campaign.status === 'ACTIVE' ? <button onClick={() => pause.mutate(campaign._id)} className="inline-flex items-center gap-2 rounded-lg border px-3 py-2 text-sm"><Pause size={15} /> Pause</button> : campaign.status === 'PAUSED' ? <button onClick={() => resume.mutate(campaign._id)} className="inline-flex items-center gap-2 rounded-lg border px-3 py-2 text-sm"><Play size={15} /> Resume</button> : null}</div>)}</div> : <p className="mt-3 text-sm text-gray-500">No Ruby Quest campaigns yet.</p>}</section></main>;
}

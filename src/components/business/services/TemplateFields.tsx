'use client';
export function TemplateFields({ value,onChange }:{value:Record<string,string>;onChange:(value:Record<string,string>)=>void}){const rows=Object.entries(value);return <div className="space-y-2">{rows.map(([key,val])=><div key={key} className="flex gap-2"><input value={key} onChange={e=>{const n={...value};delete n[key];n[e.target.value]=val;onChange(n)}} placeholder="Field" className="flex-1 rounded border p-2 text-sm"/><input value={val} onChange={e=>onChange({...value,[key]:e.target.value})} placeholder="Value" className="flex-1 rounded border p-2 text-sm"/></div>)}<button type="button" onClick={()=>onChange({...value,[`Field ${rows.length+1}`]:''})} className="text-sm font-semibold text-ruby-red">+ Add custom field</button></div>}
